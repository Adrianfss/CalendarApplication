<Window x:Class="CalendarApplication.Views.ErrorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalendarApplication.Views"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="ErrorView" Height="300" Width="400">
    <Grid HorizontalAlignment="Right" Width="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="118*"/>
            <RowDefinition Height="167*"/>
        </Grid.RowDefinitions>
        <Label Name="Message" Content="" HorizontalAlignment="Center" VerticalAlignment="Center"  Grid.Row="0"/>
        <Button x:Name="CloseWindow" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Ok"   Grid.Row="1"/>

    </Grid>
</Window>
